apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: {{ .Release.Namespace }}
  labels:
    abcdesktop/role: mongodb
spec:
  ports:
  - protocol: TCP
    port: 27017
    targetPort: 27017
    name: mongodb
  clusterIP: None
  selector:
    run: mongodb-od 
